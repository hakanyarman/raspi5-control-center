# Copy this file to .env and replace the password before first startup.
# Never commit the resulting .env file.
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=raspi
POSTGRES_PASSWORD=replace_with_a_long_random_password
POSTGRES_DB=control_center
APP_TIMEZONE=Europe/Istanbul
FILES_ROOT=/home/hakanyarman/control-center-files
CAMERA_STREAM_NAME=room

# Keep the API loopback-only unless authentication and exposure are reviewed.
API_HOST=127.0.0.1
API_PORT=3001
